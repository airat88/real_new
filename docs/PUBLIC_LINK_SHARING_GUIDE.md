# Руководство по интеграции улучшенного функционала публичных ссылок

## 📋 Обзор

Этот функционал позволяет брокеру создавать публичные ссылки на подборки объектов и отправлять их незарегистрированным пользователям через различные каналы.

## ✨ Новые возможности

1. **QR-код** - автоматическая генерация QR-кода для каждой подборки
2. **Быстрая отправка** - кнопки для отправки через WhatsApp, Telegram, Email, SMS
3. **Скачивание QR** - возможность сохранить QR-код как изображение
4. **Улучшенный UI** - красивое модальное окно с информацией о подборке
5. **Предзаполненные сообщения** - готовые шаблоны для отправки клиентам

## 🚀 Установка

### Шаг 1: Добавить библиотеку QR-кода

В файле `src/broker/properties.html` в секции `<head>` (после строки 30) добавьте:

```html
<!-- QR Code Generator -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
```

### Шаг 2: Заменить модальное окно

Замените существующий блок модального окна `linkModal` (строки 985-1024) на код из файла `improved_link_modal.html`.

**Было:**
```html
<div class="modal-overlay" id="linkModal">
    <div class="modal">
        <div class="modal__header">
            <h3 class="modal__title">Selection Created!</h3>
            <!-- ... старый код ... -->
        </div>
    </div>
</div>
```

**Стало:**
Полный новый код из `improved_link_modal.html`

### Шаг 3: Обновить функцию confirmCreateSelection

В файле `src/broker/properties.html` найдите функцию `confirmCreateSelection` (примерно строка 1850).

После строки:
```javascript
document.getElementById('linkModal').classList.add('modal-overlay--open');
```

Добавьте:
```javascript
// Обновить информацию о подборке
document.getElementById('shareLinkPropertyCount').textContent = selectedIds.length;
document.getElementById('shareLinkClientName').textContent = clientName || 'Client';
document.getElementById('shareLinkCreatedAt').textContent = new Date().toLocaleDateString('en-GB', {
    day: '2-digit',
    month: 'short',
    year: 'numeric'
});

// Сгенерировать QR-код
generateQRCode(result.link);
```

## 📱 Как это работает

### 1. Создание подборки

Брокер:
1. Выбирает объекты на странице Properties
2. Нажимает "Create Selection"
3. Заполняет информацию о клиенте
4. Получает публичную ссылку

### 2. Отправка клиенту

Брокер может отправить ссылку несколькими способами:

#### WhatsApp
- Нажимает кнопку "WhatsApp"
- Открывается WhatsApp с готовым сообщением
- Выбирает контакт и отправляет

Шаблон сообщения:
```
Hi [Client Name]! 👋

I've prepared a selection of [N] properties for you.

Click here to view: [LINK]

Swipe right ❤️ for properties you like, left 👎 for ones you don't.

Looking forward to your feedback!
```

#### Telegram
- Нажимает кнопку "Telegram"
- Открывается Telegram с ссылкой
- Выбирает контакт и отправляет

#### Email
- Нажимает кнопку "Email"
- Открывается почтовый клиент с готовым письмом
- Вводит email клиента и отправляет

#### SMS
- Нажимает кнопку "SMS"
- Открывается приложение SMS
- Выбирает номер и отправляет

#### QR-код
- Показывает QR-код клиенту на экране
- Клиент сканирует камерой телефона
- Автоматически открывается подборка

Или:
- Скачивает QR-код как изображение
- Отправляет изображение любым способом

### 3. Просмотр клиентом

Клиент (незарегистрированный):
1. Переходит по ссылке или сканирует QR
2. Открывается страница `/client/swipe.html?t={token}`
3. Видит подборку в формате Tinder-свайпов
4. Свайпает вправо ❤️ (нравится) или влево 👎 (не нравится)
5. Реакции сохраняются в базе данных

### 4. Получение обратной связи

Брокер:
1. Открывает раздел Selections
2. Видит статистику по каждой подборке
3. Видит, какие объекты понравились клиенту
4. Связывается с клиентом для следующих шагов

## 🎨 Дизайн и UX

### Модальное окно

**Цвета:**
- Заголовок: Gradient purple (#667eea → #764ba2)
- Успех: Green (#25D366 для WhatsApp)
- Предупреждение: Amber (#F59E0B)

**Секции:**
1. **Заголовок** - градиентный фон с эмодзи 🎉
2. **Информация** - краткая сводка о подборке
3. **Предупреждение** - напоминание о сроке действия (7 дней)
4. **Ссылка** - текстовое поле с кнопкой копирования
5. **QR-код** - визуальный код для сканирования
6. **Быстрая отправка** - кнопки мессенджеров
7. **PDF экспорт** - кнопки для трех языков

### Адаптивность

Модальное окно адаптируется под разные размеры экрана:
- Desktop: 600px ширина
- Tablet: 90% ширины экрана
- Mobile: 95% ширины экрана

## 🔒 Безопасность

### Токены
- Генерируются случайно (12 символов)
- Хранятся в Supabase
- Уникальны для каждой подборки

### Срок действия
- По умолчанию: 7 дней
- Настраивается в базе данных (поле `expires_at`)
- После истечения ссылка не работает

### Приватность
- Ссылку может открыть только тот, у кого есть токен
- Нет публичного списка всех подборок
- Реакции привязаны к конкретной подборке

## 📊 Аналитика (будущее расширение)

Можно добавить отслеживание:
- Количество открытий ссылки
- Время просмотра
- Устройство пользователя
- География просмотров

Для этого добавьте в таблицу `selections` поля:
```sql
ALTER TABLE selections
ADD COLUMN views_count INTEGER DEFAULT 0,
ADD COLUMN last_viewed_at TIMESTAMP,
ADD COLUMN view_details JSONB;
```

И обновите клиентскую страницу для отправки события просмотра.

## 🐛 Решение проблем

### QR-код не генерируется

**Проблема:** QR-код не появляется в модальном окне

**Решение:**
1. Проверьте, что библиотека qrcode.js загружена
2. Откройте консоль браузера на наличие ошибок
3. Убедитесь, что функция `generateQRCode()` вызывается

```javascript
// Проверка в консоли
console.log('QRCode library:', typeof QRCode);
```

### Кнопки отправки не работают

**Проблема:** Клик на WhatsApp/Telegram не открывает приложение

**Решение:**
1. На desktop могут не быть установлены приложения
2. Используйте web-версии (работает для Telegram)
3. Для WhatsApp убедитесь в формате ссылки

### Ссылка не копируется

**Проблема:** При клике на "Copy" ничего не происходит

**Решение:**
1. Проверьте поддержку `navigator.clipboard` в браузере
2. Сайт должен работать по HTTPS
3. Fallback на старый метод:

```javascript
// Альтернативный метод копирования
input.select();
document.execCommand('copy');
```

## 📝 Кастомизация

### Изменить текст сообщений

Отредактируйте функции в скрипте:

```javascript
function shareViaWhatsApp() {
    const message = encodeURIComponent(
        `Ваш текст здесь...`
    );
    // ...
}
```

### Изменить срок действия ссылки

В `supabase-client.js`:

```javascript
async createSelection(name, propertyIds, description = '', clientId = null) {
    // ...
    const expiresAt = new Date();
    expiresAt.setDate(expiresAt.getDate() + 14); // 14 дней вместо 7
    
    insertData.expires_at = expiresAt.toISOString();
}
```

### Добавить свои каналы отправки

Добавьте новую кнопку:

```html
<button class="share-btn share-btn--viber" onclick="shareViaViber()">
    <span>📞</span>
    <span>Viber</span>
</button>
```

И функцию:

```javascript
function shareViaViber() {
    const link = document.getElementById('selectionLink').value;
    window.open(`viber://forward?text=${encodeURIComponent(link)}`, '_blank');
}
```

## 🎯 Рекомендации

1. **Тестируйте** перед отправкой клиентам
   - Используйте кнопку "Preview"
   - Проверьте на мобильном устройстве

2. **Персонализируйте** сообщения
   - Измените шаблоны под свой стиль
   - Добавьте контактную информацию

3. **Отслеживайте** обратную связь
   - Регулярно проверяйте раздел Selections
   - Своевременно реагируйте на реакции клиентов

4. **Используйте** QR-коды
   - Идеально для личных встреч
   - Можно распечатать на визитках

## 📚 Дополнительная информация

### Структура URL

Публичная ссылка имеет формат:
```
https://yourdomain.com/client/swipe.html?t=abc123def456
```

Где:
- `yourdomain.com` - ваш домен
- `/client/swipe.html` - страница просмотра
- `t=abc123def456` - уникальный токен подборки

### База данных

Таблица `selections`:
```
id              UUID PRIMARY KEY
broker_id       UUID REFERENCES brokers(id)
client_id       UUID REFERENCES clients(id) NULL
name            TEXT NOT NULL
description     TEXT
token           TEXT UNIQUE NOT NULL
property_ids    TEXT[] NOT NULL
total_properties INTEGER
status          TEXT (pending|active|completed|expired)
created_at      TIMESTAMP DEFAULT NOW()
expires_at      TIMESTAMP
completed_at    TIMESTAMP NULL
```

Таблица `reactions`:
```
id              UUID PRIMARY KEY
selection_id    UUID REFERENCES selections(id)
property_id     TEXT NOT NULL
property_title  TEXT
reaction        TEXT (like|dislike)
created_at      TIMESTAMP DEFAULT NOW()
```

## 🔄 Версионность

**Версия:** 1.0.0  
**Дата:** Декабрь 2024  
**Автор:** Cyprus Real Estate Platform

### История изменений

- **1.0.0** - Первый релиз
  - QR-код генерация
  - Быстрая отправка через мессенджеры
  - Улучшенный UI
  - Шаблоны сообщений

## 💡 Идеи для улучшений

1. **Короткие ссылки** - интеграция с bit.ly или собственный сервис
2. **Аналитика** - отслеживание просмотров и взаимодействий
3. **Уведомления** - push/email когда клиент просмотрел подборку
4. **Комментарии** - возможность клиента оставить комментарий к объекту
5. **Избранное** - клиент может сохранить подборку в избранное
6. **Сравнение** - side-by-side сравнение понравившихся объектов

## 🆘 Поддержка

При возникновении вопросов или проблем:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все зависимости загружены
3. Проверьте конфигурацию Supabase
4. Создайте issue в репозитории проекта

# 🔧 Исправления для мобильного интерфейса подборок недвижимости

## 📋 Обзор исправлений

Этот пакет содержит исправления для трёх критических проблем:

1. ✅ **Модальное окно не помещается на экране телефона** - исправлена адаптивность
2. ✅ **Кнопка "View on Website" заменена на "Позвонить брокеру"** - интеграция с телефоном
3. ✅ **Добавлена поддержка Viber** - теперь все способы связи отображаются

---

## 📁 Структура файлов

```
fixed_files/
├── src/
│   ├── assets/
│   │   ├── css/
│   │   │   └── swipe.css          [ИЗМЕНЁН] - Мобильная адаптация модального окна
│   │   └── js/
│   │       └── swipe.js           [ИЗМЕНЁН] - Замена кнопки на "Позвонить брокеру"
│   ├── client/
│   │   └── property.html          [ИЗМЕНЁН] - Замена кнопки на "Позвонить брокеру"
│   └── broker/
│       └── selections.html        [ИЗМЕНЁН] - Добавлен Viber + улучшена адаптация
│
├── MOBILE_FIXES_RU.md              [ДОКУМЕНТАЦИЯ] - Подробное описание всех изменений
├── broker_phone_integration.js     [ИНСТРУКЦИИ] - Как интегрировать телефон брокера
└── README.md                       [ЭТО] - Быстрое руководство
```

---

## 🚀 Быстрая установка

### Шаг 1: Замена файлов

Скопируйте исправленные файлы в ваш проект:

```bash
# Из директории с исправлениями
cp -r fixed_files/* /путь/к/вашему/проекту/
```

Или вручную замените каждый файл:
- `src/assets/css/swipe.css`
- `src/assets/js/swipe.js`
- `src/client/property.html`
- `src/broker/selections.html`

### Шаг 2: Добавление телефона брокера

#### Вариант A: Обновить существующие подборки в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в таблицу `selections`
3. Добавьте новую колонку `broker_phone` (тип: text)
4. Обновите существующие записи, добавив номер телефона брокера

Пример SQL:
```sql
-- Добавить колонку
ALTER TABLE selections 
ADD COLUMN broker_phone TEXT;

-- Обновить существующие записи
UPDATE selections 
SET broker_phone = '+35799123456' 
WHERE broker_id = 'ваш_broker_id';
```

#### Вариант B: Обновить код создания подборок

В файле `src/broker/selections.html` найдите место где создаются новые подборки и добавьте поле `broker_phone`:

```javascript
// Пример: при создании новой подборки
const newSelection = {
    name: selectionName,
    property_ids: selectedIds,
    broker_id: userId,
    broker_phone: '+35799123456', // ⬅️ ДОБАВИТЬ ЭТУ СТРОКУ
    token: generateToken(),
    created_at: new Date().toISOString()
};
```

### Шаг 3: Проверка

Откройте подборку на мобильном устройстве и проверьте:

- ✅ Модальное окно открывается и помещается на экране
- ✅ Кнопка "📞 Позвонить брокеру" отображается (если добавлен broker_phone)
- ✅ При создании подборки отображаются все варианты отправки: WhatsApp, Viber, Email, Telegram, QR
- ✅ На маленьких экранах (<480px) кнопки расположены в один столбец

---

## 📱 Детальное описание изменений

### 1. Мобильная адаптация модального окна (`swipe.css`)

#### Что изменено:
- Увеличена максимальная высота модального окна: `98vh` для очень маленьких экранов, `95vh` для средних
- Модальное окно теперь фиксируется внизу экрана для лучшего UX на мобильных
- Ограничена высота изображений для экономии места
- Уменьшены отступы для компактности

#### Добавленные медиа-запросы:

```css
/* Для экранов < 480px */
@media (max-width: 480px) {
    .details-modal__content {
        max-height: 98vh;
        position: fixed;
        bottom: 0;
    }
    .details-gallery__main img {
        max-height: 35vh;
    }
}

/* Для экранов 481px - 768px */
@media (max-width: 768px) and (min-width: 481px) {
    .details-modal__content {
        max-height: 95vh;
    }
    .details-gallery__main img {
        max-height: 40vh;
    }
}
```

---

### 2. Замена кнопки "View on Website" на "Позвонить брокеру"

#### Изменённые файлы:
- `src/client/property.html` (строки 385-396)
- `src/assets/js/swipe.js` (строки 612-623)

#### Старый код:
```html
${prop.url ? `
<div style="margin-top: 20px;">
    <a href="${prop.url}" target="_blank" class="btn btn-primary">
        🔗 View on Website
    </a>
</div>
` : ''}
```

#### Новый код:
```html
${prop.broker_phone || prop.brokerPhone ? `
<div style="margin-top: 20px;">
    <a href="tel:${prop.broker_phone || prop.brokerPhone}" class="btn btn-primary">
        📞 Позвонить брокеру
    </a>
</div>
` : ''}
```

**Как это работает:**
- Если у объекта есть поле `broker_phone`, отображается кнопка "Позвонить брокеру"
- При нажатии открывается телефонное приложение с номером брокера
- На десктопе работает с программами типа Skype

---

### 3. Добавлена поддержка Viber (`selections.html`)

#### Новые функции:

```javascript
// Общий Viber share
function shareViber(link, name, phone = null) {
    const text = encodeURIComponent(`Посмотрите подборку: ${name}\n\n${link}`);
    if (phone) {
        window.open(`viber://chat?number=${phone}&text=${text}`, '_blank');
    } else {
        window.open(`viber://forward?text=${text}`, '_blank');
    }
}

// Отправка конкретному клиенту
function shareViberDirect(link, name, phone, clientName) {
    const text = encodeURIComponent(
        `Здравствуйте, ${clientName}!\n\nПодборка: ${name}\n\n${link}`
    );
    window.open(`viber://chat?number=${phone}&text=${text}`, '_blank');
}
```

#### Где добавлено:
В функции `showShareModal()` теперь генерируется кнопка Viber:

```javascript
// Viber button
if (clientPhone) {
    shareOptionsHTML += `
        <button onclick="shareViberDirect(...)" class="share-btn">
            📱 Viber
            <div style="font-size: 0.75rem;">${clientName}</div>
        </button>
    `;
} else {
    shareOptionsHTML += `
        <button onclick="shareViber(...)" class="share-btn">
            📱 Viber
        </button>
    `;
}
```

#### Мобильная адаптация share modal:

```css
@media (max-width: 480px) {
    .share-options {
        grid-template-columns: 1fr; /* Кнопки в один столбец */
    }
    .share-modal-content {
        width: 95%;
        max-height: 85vh;
    }
}
```

---

## 🎨 Визуальные улучшения

### Кнопка "Позвонить брокеру"
- Градиентный фон: `#667eea → #764ba2`
- Крупнее и заметнее
- Тень для глубины
- Скруглённые углы
- Иконка телефона 📞

### Share modal на мобильных
- Кнопки в один столбец на маленьких экранах
- Увеличенная высота для лучшей видимости
- Плавная прокрутка на iOS
- Компактные отступы

---

## 🐛 Решение проблем

### Проблема: Кнопка "Позвонить брокеру" не отображается

**Решение:**
1. Убедитесь что в таблице `selections` есть колонка `broker_phone`
2. Проверьте что значение заполнено: `SELECT broker_phone FROM selections LIMIT 10;`
3. Убедитесь что свойства получают это поле при загрузке

**Проверка в консоли:**
```javascript
// В swipe.js добавлен console.log
console.log('📞 Broker phone:', this.brokerPhone);
```

### Проблема: Viber не открывается

**Причины:**
- У пользователя не установлен Viber
- Номер телефона в неправильном формате

**Решение:**
Номер должен быть в формате: `+357XXXXXXXXX` (с кодом страны)

### Проблема: Модальное окно всё ещё не помещается

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте что файл `swipe.css` обновлён
3. Убедитесь что нет конфликтующих стилей

**Проверка:**
```javascript
// В консоли браузера
getComputedStyle(document.querySelector('.details-modal__content')).maxHeight
// Должно быть: "98vh" на мобильных
```

---

## 📊 Тестовые сценарии

### ✅ Тест 1: Модальное окно на мобильном
1. Откройте подборку на телефоне
2. Нажмите на любой объект
3. Проверьте что модальное окно открывается снизу
4. Проверьте что весь контент виден без прокрутки (или прокручивается плавно)

### ✅ Тест 2: Кнопка "Позвонить брокеру"
1. Откройте подборку с broker_phone
2. Нажмите на объект → откроется модальное окно
3. Проверьте наличие кнопки "📞 Позвонить брокеру"
4. Нажмите кнопку → должно открыться телефонное приложение

### ✅ Тест 3: Share modal на мобильном
1. В broker panel создайте подборку
2. Нажмите "Поделиться"
3. Проверьте что все 5 кнопок видны: WhatsApp, Email, Viber, Telegram, QR
4. На экране <480px кнопки должны быть в один столбец

### ✅ Тест 4: Viber
1. Создайте подборку с указанным телефоном клиента
2. Нажмите "Поделиться" → "Viber"
3. Должен открыться Viber с предзаполненным сообщением

---

## 💡 Дополнительные советы

### Оптимизация номера телефона

Рекомендуемый формат для Кипра:
```
+35799123456
```

Функция очистки номера уже встроена:
```javascript
const cleanPhone = clientPhone.replace(/[^0-9+]/g, '');
```

### Добавление телефона брокера в профиль

Если вы используете демо-аккаунты в `login.html`, добавьте телефон:

```javascript
const DEMO_ACCOUNTS = [
    {
        email: 'broker@example.com',
        password: 'demo123',
        name: 'Demo Broker',
        phone: '+35799123456' // ⬅️ ДОБАВИТЬ
    }
];
```

Затем при создании подборки передавайте этот телефон.

---

## 📞 Поддержка

Если у вас возникли проблемы после установки исправлений:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь что все файлы заменены правильно
3. Очистите кэш браузера
4. Проверьте что Supabase содержит поле `broker_phone`

---

## 📝 Changelog

**v1.0.0 - 2025-12-16**
- ✅ Исправлена адаптация модального окна для мобильных устройств
- ✅ Заменена кнопка "View on Website" на "Позвонить брокеру"
- ✅ Добавлена поддержка Viber для отправки подборок
- ✅ Улучшена мобильная адаптация share modal
- ✅ Добавлены медиа-запросы для экранов <480px и 481-768px

---

## ✨ Что дальше?

Рекомендуемые улучшения для будущих версий:

1. **Email подпись брокера** - добавить автоматическую подпись в email с контактами
2. **История звонков** - логирование когда клиент нажимает "Позвонить брокеру"
3. **SMS поддержка** - добавить кнопку отправки SMS с подборкой
4. **WhatsApp Business API** - интеграция для отправки через бизнес-аккаунт
5. **Аналитика** - отслеживание какие способы связи используются чаще

---

**Готово! 🎉** 

Все исправления установлены. Наслаждайтесь улучшенным мобильным интерфейсом!

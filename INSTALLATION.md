# Инструкция по внедрению обновлений

## Быстрый старт

Все изменения уже внесены в код. Для внедрения:

### 1. Замените файлы на сервере

Скопируйте обновленные файлы:

```
src/assets/js/pdf-export.js    (обновлен)
src/assets/js/swipe.js         (обновлен)
src/broker/selections.html     (обновлен)
src/client/property.html       (НОВЫЙ)
```

### 2. Настройте брендирование

В файле `src/broker/selections.html` найдите функцию `exportResultsPDF` (строка ~1269):

```javascript
function exportResultsPDF(lang) {
    PDFExport.generateResultsPDF({
        selectionName: currentDetailSelection.name,
        clientName: currentDetailSelection.clientName,
        brokerName: 'Cyprus Real Estate',        // ← Измените на название вашей компании
        brokerLicense: '1234/E',                 // ← Измените на ваш номер лицензии
        brokerLogo: null,                        // ← Добавьте логотип (см. ниже)
        properties: currentDetailSelection.properties,
        reactions: currentDetailSelection.reactions,
        lang: lang,
        showForBroker: true
    });
}
```

### 3. Добавление логотипа (опционально)

Чтобы добавить логотип компании в PDF:

1. Конвертируйте ваш логотип в base64
2. Замените `null` на строку base64:

```javascript
brokerLogo: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...',
```

**Онлайн конвертеры:**
- https://www.base64-image.de/
- https://base64.guru/converter/encode/image

**Рекомендации:**
- Размер: 200x60 пикселей
- Формат: PNG с прозрачностью
- Вес: < 50 KB

### 4. Проверка работоспособности

После внедрения проверьте:

#### ✅ Swipe интерфейс клиента:
1. Откройте любую подборку
2. Проверьте, что на карточках показываются две цены:
   - €350,000
   - €416,500 incl. VAT

#### ✅ PDF экспорт:
1. Откройте Selections → детали подборки
2. Нажмите "Export Results" → выберите язык
3. Проверьте PDF:
   - Есть логотип и лицензия вверху
   - Показаны обе цены (с НДС и без)
   - ID объектов видны (для брокера)

#### ✅ Страница объекта:
1. Откройте `/src/client/property.html?id=CYP-001234`
2. Проверьте отображение цен с НДС
3. Проверьте брендирование внизу страницы

## Кастомизация

### Изменить ставку НДС

Если нужна другая ставка НДС (не 19%), измените в файлах:

**pdf-export.js** (строка ~102):
```javascript
VAT_RATE: 0.19,  // Измените на нужную ставку (например, 0.20 для 20%)
```

**swipe.js** (строка ~16):
```javascript
this.VAT_RATE = 0.19;  // Измените на нужную ставку
```

**property.html** (строка ~157):
```javascript
const VAT_RATE = 0.19;  // Измените на нужную ставку
```

### Скрыть цену с НДС

Если не нужно показывать цену с НДС клиентам:

**swipe.js** - закомментируйте строку с VAT:
```javascript
<div class="property-card__price">
    <div>${priceWithoutVAT}</div>
    <!-- <div style="...">${priceWithVAT} incl. VAT</div> -->
</div>
```

### Создать две версии PDF (для брокера и клиента)

Добавьте две кнопки экспорта в `selections.html`:

```javascript
// Для брокера (с ID объектов)
function exportForBroker(lang) {
    PDFExport.generateResultsPDF({
        // ... параметры
        showForBroker: true
    });
}

// Для клиента (без ID)
function exportForClient(lang) {
    PDFExport.generateResultsPDF({
        // ... параметры
        showForBroker: false
    });
}
```

В HTML добавьте кнопки:
```html
<button onclick="exportForBroker('ru')">Export for Broker</button>
<button onclick="exportForClient('ru')">Export for Client</button>
```

## Обновление базы данных

Изменения в коде не требуют обновления базы данных Supabase.

## Поддержка

### Частые вопросы

**Q: Как загрузить логотип через интерфейс?**  
A: В текущей версии логотип нужно добавлять в код. В будущем планируется добавить загрузку через настройки.

**Q: Можно ли использовать разные лицензии для разных брокеров?**  
A: Да, в будущей версии это будет храниться в профиле брокера в БД.

**Q: Работает ли НДС для валют кроме евро?**  
A: Да, расчет работает с любыми числовыми значениями.

**Q: Где хранятся внутренние ссылки?**  
A: Ссылки генерируются динамически при создании PDF, используя ID объекта.

### Контакты

Если возникли проблемы при внедрении, создайте issue в репозитории с описанием проблемы.

## Changelog

**v0.3.0 (06.12.2024)**
- ✅ Добавлен расчет и отображение НДС (19%)
- ✅ Брендирование PDF (логотип + лицензия)
- ✅ Скрытие ID объектов от клиентов
- ✅ Внутренние ссылки на объекты
- ✅ Создана страница просмотра объекта

---

**Готово к работе!** 🎉

# 📞 Инструкция по настройке телефона брокера

## Обзор изменений

В проект добавлена полная поддержка номера телефона брокера для всех подборок. Теперь:

✅ Номер телефона брокера сохраняется при создании подборки  
✅ Клиенты видят кнопку "📞 Позвонить брокеру" на каждом объекте  
✅ Звонок происходит напрямую из браузера/приложения  

---

## Быстрый старт

### Шаг 1: Убедитесь что в Supabase есть поле `broker_phone`

Выполните SQL запрос в Supabase Dashboard:

```sql
-- Проверить существует ли колонка
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'selections' 
  AND column_name = 'broker_phone';

-- Если колонки нет, добавить её
ALTER TABLE selections 
ADD COLUMN IF NOT EXISTS broker_phone TEXT;
```

### Шаг 2: Установите ваш номер телефона в системе

1. Откройте `src/broker/properties.html`
2. Нажмите на иконку ⚙️ Settings в правом верхнем углу
3. В поле "Broker Phone" введите ваш номер в формате: `+357 99 123456`
4. Нажмите "Save Settings"

Номер сохранится в localStorage браузера.

### Шаг 3: Создайте новую подборку

После установки номера телефона:

1. Выберите объекты недвижимости
2. Создайте подборку как обычно
3. Номер телефона автоматически добавится к подборке

---

## Формат номера телефона

### Рекомендуемый формат для Кипра:

```
+357 99 123456
+35799123456
```

### Другие страны:

```
+1 555 123 4567    (USA)
+44 20 7123 4567   (UK)
+7 495 123 4567    (Russia)
```

**Важно:** Всегда используйте `+` в начале номера для международного формата.

---

## Обновление существующих подборок

Если у вас уже есть подборки БЕЗ номера телефона, обновите их через SQL:

```sql
-- Обновить все подборки одного брокера
UPDATE selections 
SET broker_phone = '+357 99 123456' 
WHERE broker_id = '00000000-0000-0000-0000-000000000001';

-- Или обновить конкретную подборку
UPDATE selections 
SET broker_phone = '+357 99 123456' 
WHERE id = 'your-selection-id';
```

---

## Проверка работы

### Тест 1: Создание новой подборки

1. Установите номер телефона в настройках
2. Создайте новую подборку
3. Откройте консоль браузера (F12)
4. Проверьте лог: `"Selection created successfully"` должен содержать `broker_phone`

### Тест 2: Просмотр клиентом

1. Откройте ссылку на подборку как клиент
2. Нажмите на любой объект
3. Проверьте наличие кнопки "📞 Позвонить брокеру"
4. Нажмите кнопку - должен открыться телефон

### Тест 3: Проверка в базе данных

```sql
-- Проверить что номер сохранён
SELECT id, name, broker_phone, created_at 
FROM selections 
ORDER BY created_at DESC 
LIMIT 5;
```

---

## Технические детали

### Где хранится номер телефона

1. **localStorage браузера брокера:**
   - Ключ: `broker_phone`
   - Значение: номер телефона

2. **Таблица Supabase `selections`:**
   - Колонка: `broker_phone`
   - Тип: TEXT

### Как передаётся номер телефона

```javascript
// 1. При создании подборки (properties.html)
const brokerPhone = localStorage.getItem('broker_phone');
await SupabaseClient.createSelection(name, ids, desc, clientId, brokerPhone);

// 2. В Supabase (supabase-client.js)
insertData.broker_phone = brokerPhone;

// 3. При загрузке клиентом (swipe.js)
this.properties = this.properties.map(p => ({
    ...p,
    broker_phone: selection.broker_phone
}));

// 4. Отображение кнопки
${prop.broker_phone ? `
    <a href="tel:${prop.broker_phone}">📞 Позвонить брокеру</a>
` : ''}
```

---

## Устранение проблем

### Проблема: Кнопка не отображается

**Причина:** Номер телефона не установлен или не передан при создании подборки.

**Решение:**
1. Проверьте localStorage: `localStorage.getItem('broker_phone')`
2. Установите номер через Settings
3. Создайте новую подборку (старые не обновятся автоматически)

### Проблема: Звонок не работает

**Причина:** Неправильный формат номера или устройство не поддерживает `tel:` ссылки.

**Решение:**
1. Используйте международный формат с `+`
2. Проверьте на мобильном устройстве (на десктопе работает через Skype/FaceTime)

### Проблема: Старые подборки без номера

**Причина:** Подборки созданы до добавления функции.

**Решение:**
```sql
UPDATE selections 
SET broker_phone = '+357 99 123456' 
WHERE broker_phone IS NULL;
```

---

## Дополнительные возможности

### Разные номера для разных клиентов

В будущем можно расширить функциональность:

```javascript
// При создании подборки передавать специфический номер
const specificPhone = '+357 99 999999'; // для VIP клиента
await SupabaseClient.createSelection(name, ids, desc, clientId, specificPhone);
```

### Tracking звонков

Можно добавить аналитику:

```javascript
// При клике на кнопку "Позвонить"
analytics.track('broker_call_initiated', {
    selection_id: selectionId,
    property_id: propertyId,
    broker_phone: brokerPhone
});
```

---

## Checklist установки

- [ ] Проверено что колонка `broker_phone` существует в Supabase
- [ ] Установлен номер телефона в Settings (properties.html)
- [ ] Создана тестовая подборка
- [ ] Проверено что номер сохранён в базе данных
- [ ] Открыта ссылка как клиент
- [ ] Проверено наличие кнопки "Позвонить брокеру"
- [ ] Проверено что клик открывает телефон
- [ ] Обновлены старые подборки (если нужно)

---

## Контакты поддержки

Если возникли проблемы:

1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте что Supabase доступен
3. Убедитесь что номер в правильном формате
4. Попробуйте на другом устройстве/браузере

**Всё готово! 🎉**
